# OmniAuth::Brightspace

OmniAuth strategy for Brightspace

## Installation

Add this line to your application's Gemfile:

```ruby
gem 'omniauth-brightspace', git: 'https://github.com/mconf/omniauth-brightspace'
```

And then run:

    $ bundle
